{
  "name": "ainetscape",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI Netscape — ainetscape.com",
  "scripts": {
    "dev": "vercel dev",
    "build:favicons": "python3 scripts/build_favicons.py",
    "build:og": "node scripts/build_og.mjs",
    "build:ga": "node scripts/build_ga.mjs",
    "build:pages": "node scripts/build_pages.mjs",
    "build:assets": "npm run build:favicons && npm run build:og",
    "vercel-build": "npm run build:ga && npm run build:pages"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "marked": "^18.0.4",
    "playwright": "^1.49.1"
  }
}
